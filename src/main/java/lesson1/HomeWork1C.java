package lesson1;

public class HomeWork1C {
    public static void main(String[] args) {

        double a = 1.1;
        double b = 2.1;
        double c = 3.1;
        double d = 4.1;
        double e = 5.1;

        double result = (double) a * b * c * d * e;
        System.out.println("Number " + a +  " * number " + b + " * number " + c + " * number " + d + " * number " + e +
                " equals " + result);
    }
}
